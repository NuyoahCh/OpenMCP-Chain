1. Get the application URL by running these commands:
{{- if .Values.ingress.enabled }}
  http{{ if .Values.ingress.tls }}s{{ end }}://{{ (index .Values.ingress.hosts 0).host }}
{{- else if eq .Values.service.type "LoadBalancer" }}
  Watch for the external IP: kubectl get svc {{ include "openmcp-chain.fullname" . }} -n {{ .Release.Namespace }}
{{- else }}
  Port-forward the service:
  kubectl port-forward svc/{{ include "openmcp-chain.fullname" . }} 8080:{{ .Values.service.port }} -n {{ .Release.Namespace }}
{{- end }}
